<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.baofu.cbpayservice.dal.mapper.FiCbPayOrderTicketsMapper">

  <resultMap id="BaseResultMap" type="com.baofu.cbpayservice.dal.models.FiCbPayOrderTicketsDo">
    <id     column="ID"                     jdbcType="BIGINT"               property="id" />
    <result column="ORDER_ID"               jdbcType="BIGINT"               property="orderId" />
    <result column="FLIGHT_NUMBER"          jdbcType="VARCHAR"              property="flightNumber" />
    <result column="ADDRESS"                jdbcType="VARCHAR"              property="address" />
    <result column="TAKEOFF_TIME"           jdbcType="TIMESTAMP"            property="takeoffTime" />
    <result column="CREATE_AT"              jdbcType="TIMESTAMP"            property="createAt" />
    <result column="CREATE_BY"              jdbcType="VARCHAR"              property="createBy" />
    <result column="UPDATE_AT"              jdbcType="TIMESTAMP"            property="updateAt" />
    <result column="UPDATE_BY"              jdbcType="VARCHAR"              property="updateBy" />
  </resultMap>

    <sql id="Base_Column_List">
        ID,
        ORDER_ID,
        FLIGHT_NUMBER,
        ADDRESS,
        TAKEOFF_TIME,
        CREATE_AT,
        CREATE_BY,
        UPDATE_AT,
        UPDATE_BY
    </sql>

    <!-- 批量添加航旅信息 -->
    <insert id="batchInsert" parameterType="com.baofu.cbpayservice.dal.models.FiCbPayOrderTicketsDo">
        /*FiCbPayOrderTicketsMapper.batchInsert*/
        insert into FI_CBPAY_ORDER_TICKETS (
        ORDER_ID,
        FLIGHT_NUMBER,
        ADDRESS,
        TAKEOFF_TIME,
        CREATE_AT,
        CREATE_BY,
        UPDATE_AT,
        UPDATE_BY
        ) values
        <foreach collection="list" item="item" index="index" separator=",">
            (
            #{item.orderId,jdbcType=BIGINT},
            #{item.flightNumber,jdbcType=VARCHAR},
            #{item.address,jdbcType=VARCHAR},
            #{item.takeoffTime,jdbcType=TIMESTAMP},
            NOW(),
            #{item.createBy,jdbcType=VARCHAR},
            NOW(),
            #{item.updateBy,jdbcType=VARCHAR}
            )
        </foreach>
    </insert>

</mapper>